receivers:
    otlp/receiver:
        protocols:
            grpc:
                endpoint: ${HTP_COLLECTOR_POD_IP}:4317
            http:
                endpoint: ${HTP_COLLECTOR_POD_IP}:4318
processors:
    usage: {}
connectors:
    routing_logs/routing/router:
        default_pipelines:
            - logs/production
        table:
            - pipelines:
                - logs/production
              statement: route() where attributes["deployment.environment"] == "production"
    routing_metrics/routing/router:
        default_pipelines:
            - metrics/production
        table:
            - pipelines:
                - metrics/production
              statement: route() where attributes["deployment.environment"] == "production"
    routing_traces/routing/router:
        default_pipelines:
            - traces/production
        table:
            - pipelines:
                - traces/production
              statement: route() where attributes["deployment.environment"] == "production"
exporters:
    otlphttp/exporter:
        endpoint: https://api.honeycomb.io:443
        headers:
            x-honeycomb-dataset: metrics
            x-honeycomb-team: ${HTP_EXPORTER_APIKEY}
        sending_queue:
            batch:
                flush_timeout: 200ms
                max_size: 8192
                min_size: 8192
            enabled: true
            queue_size: 100000
            sizer: items
extensions:
    honeycomb: {}
service:
    extensions: [honeycomb]
    pipelines:
        logs/a13-2a5:
            receivers: [routing-logs]
            processors: [usage]
            exporters: [otlphttp/exporter]
        logs/intake:
            receivers: [otlp/receiver]
            processors: [usage]
            exporters: [routing/router]
        metrics/8cd-301:
            receivers: [routing-metrics]
            processors: [usage]
            exporters: [otlphttp/exporter]
        metrics/intake:
            receivers: [otlp/receiver]
            processors: [usage]
            exporters: [routing/router]
        traces/a03-0e4:
            receivers: [routing-traces]
            processors: [usage]
            exporters: [otlphttp/exporter]
        traces/intake:
            receivers: [otlp/receiver]
            processors: [usage]
            exporters: [routing/router]
