kind: Dropper
name: Drop
style: dropper
type: base
status: beta
version: v0.1.0
summary: Drops all traces
description: Drops all incoming traces without sampling.
tags:
  - category:sampler
  - service:refinery
  - vendor:Honeycomb
  - type:error
  - input:SampleData
ports:
  # inputs
  - name: Sample
    direction: input
    type: SampleData
templates:
  - kind: refinery_rules
    name: DropAll_RefineryRules
    format: rules
    meta:
      sampler: RulesBasedSampler
    data:
      - key: Drop
        value: true
